import Layout from "../layout/layout";
import MainSection from "../ui/mainSection/mainSection";

const Main = () => {
  return (
    <Layout>
      <MainSection />
    </Layout>
  );
};

export default Main;
